PORT=5000
DATABASE_URL=postgres://postgres:postgres@localhost:5432/encegen_db
JWT_SECRET=encegen_super_secret_admin_jwt_key_2026
ADMIN_EMAIL=admin@encegen.com
ADMIN_PASSWORD=admin123

# SMTP config for sending job-application emails (fill in real values — e.g.
# a Gmail address + app password, or any SMTP relay like SendGrid/Mailgun).
# Applications are emailed to ADMIN_EMAIL above.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password
SMTP_FROM="Encegen Careers" <careers@encegen.com>
